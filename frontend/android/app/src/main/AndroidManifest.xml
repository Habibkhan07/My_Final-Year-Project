<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!--
        Audit C2 (F-1) — required so the tech-side foreground GPS
        broadcaster can keep streaming after the OS classifies the app
        as "background" (screen lock, app switcher, brief launcher
        excursion). Without this, Android 10+ throttles location to
        ~hourly batched updates the moment the app loses foreground
        prominence — even though the foreground service notification
        is showing.
        Android 11+ note: this permission cannot be granted via the
        runtime request dialog after the user picks "While using the
        app" once. The user MUST grant it via OS Settings → App →
        Permissions → Location → "Allow all the time".
        `ForegroundLocationServiceController.openSystemSettings()` and
        the explainer banner deep-link the tech there.
    -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <!--
        Android 13+ runtime permission for notifications. Harmless on
        targetSdk <33 (auto-granted). Runtime dialog only fires when
        targetSdk >=33 — see app/build.gradle.kts (currently
        flutter.targetSdkVersion).
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--
        Booking orchestrator session 4 — tech-side foreground GPS
        broadcaster. FOREGROUND_SERVICE_LOCATION is required on
        Android 14+ for any foreground service that uses location
        (the plain FOREGROUND_SERVICE permission is contributed by
        the flutter_foreground_task plugin's manifest merge).
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <application
        android:label="frontend"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">

        <!--
            FCM defaults applied to any incoming notification payload that
            doesn't override channel/icon/color. Channel id MUST match the
            channel registered at runtime in FCMHandler.initialize() and
            firebaseMessagingBackgroundHandler. If the channel doesn't exist
            when the first push arrives, Android routes to a "Miscellaneous"
            channel that users can't easily mute or unmute — visible-but-
            uncontrollable, the worst notification UX.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_color" />

        <!--
            Booking orchestrator session 4 — Google Maps API key.
            Resolved by `manifestPlaceholders["GOOGLE_MAPS_API_KEY"]` in
            app/build.gradle.kts (which reads the GOOGLE_MAPS_API_KEY env
            var at build time). Empty when running the OSM provider; the
            map widget logs a clear warning if `MAP_PROVIDER=google` is
            paired with an empty key (flag #16 footgun).
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${GOOGLE_MAPS_API_KEY}" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:screenOrientation="portrait"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!--
            Booking orchestrator session 4 — flutter_foreground_task
            service registration. The `name` attribute MUST exactly
            match the plugin's class — do not rename. `foregroundServiceType=
            location` declares the service category to Android 14+'s
            stricter foreground-service requirements.
        -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="location"
            android:exported="false" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.
         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
