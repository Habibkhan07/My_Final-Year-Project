<!--
    Status-bar notification icon for FCM messages. Referenced from
    AndroidManifest.xml via:
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />

    Constraint (Android API 21+): the status-bar treats this as an
    alpha-only mask — every non-transparent pixel renders as solid white,
    every fillColor is ignored at the status-bar level. The accent color
    (visible in the notification body) comes from
    @color/notification_color via the sibling default_notification_color
    meta-data.

    Glyph: wrench/spanner — Karigar's category-agnostic mark. Sourced from
    Material Symbols 'build' at 24dp, recentered to the 24x24 viewport so
    the alpha mask fills the status-bar circle cleanly (no edge clipping
    against the system circular crop).
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:fillColor="#FFFFFFFF"
        android:pathData="M22.7,19l-9.1,-9.1c0.9,-2.3 0.4,-5 -1.5,-6.9 -2,-2 -5,-2.4 -7.4,-1.3L9,6 6,9 1.6,4.7C0.4,7.1 0.9,10.1 2.9,12.1c1.9,1.9 4.6,2.4 6.9,1.5l9.1,9.1c0.4,0.4 1,0.4 1.4,0l2.3,-2.3c0.4,-0.4 0.4,-1.1 0,-1.4z" />
</vector>
