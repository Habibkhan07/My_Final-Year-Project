// DI surface for the incoming job requests feature.
//
// Currently empty — the queue notifier is the only provider and is exposed
// via codegen on its own. A repository / accept-decline data source will
// land here when the backend accept endpoint ships
// (`backend/bookings/api/BOOKINGS_API.md` §1.1).
